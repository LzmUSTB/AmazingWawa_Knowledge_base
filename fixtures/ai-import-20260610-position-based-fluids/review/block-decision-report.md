# Block Decision Report

The solver loop needs a compact node-edge diagram with selectable steps. Existing native blocks are useful for concept summaries and quizzes, but not ideal for a reusable solver pipeline graphic.

The proposed `solver-loop-diagram` block is declarative-only and uses the safe visual grammar renderer.
